commit-msg:
  parallel: true
  scripts:
    "commit-message-lint":
      runner: bash

post-commit:
  parallel: true
  scripts:
    "rbw-unlock-warning":
      runner: bash

pre-commit:
  parallel: true
  scripts:
    "protect-branch":
      runner: bash
